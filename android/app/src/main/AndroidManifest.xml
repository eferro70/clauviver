<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.clauviver.anamnese">

    <application
        android:label="ClauViver"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.SplashScreen">  <!-- 1. Tema da aplicação definido aqui -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.SplashScreen"  
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- 
              Este meta-data define o tema a ser usado APÓS a inicialização do Flutter.
              Ele garante a transição suave da splash screen para o seu app.
            -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 
          Não apague este meta-data. 
          Ele é usado pela ferramenta do Flutter para gerar o GeneratedPluginRegistrant.java.
        -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- 
      Permissões para consultar atividades que podem processar texto.
      Usado pelo motor do Flutter.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
